function hoverOver(img) {
    img.src = "images/silhouette2b.jpg"; // Change the image source on hover
}

function hoverOff(img) {
    img.src = "images/silhouette1b.jpg"; // Restore the original image source on mouseout
}
